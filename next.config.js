const path = require("path");

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  turbopack: {}, // Enable Turbopack explicitly with an empty configuration
};

module.exports = nextConfig;
